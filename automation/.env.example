# Environment Variables for Home Assistant Release Monitor

# ============================================================================
# NOTA: Questo file non è più necessario!
# ============================================================================
#
# Il sistema ora usa Claude Code CLI invece delle API dirette.
# Claude Code CLI gestisce automaticamente l'autenticazione.
#
# Se hai già installato e configurato Claude Code CLI (comando `claude`),
# non hai bisogno di configurare nessuna variabile d'ambiente.
#
# Per verificare che Claude Code CLI sia configurato:
#   claude --version
#
# ============================================================================

# Optional: Override config.yaml settings via environment variables
# BLOG_ROOT_PATH=/path/to/your/blog
# TARGET_VERSION=2026.02
# AUTO_PUSH=true
